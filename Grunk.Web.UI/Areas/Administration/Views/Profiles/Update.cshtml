@model UpdateProfilesViewModel
<div class="page-header">
    <h1>Opdater beskrivelse</h1>
</div>
@using (Html.BeginForm())
{
    @Html.TextAreaFor(x => x.Description, new { rows = "5", @class = "input-xxlarge" })
    <div class="form-actions">
        <input type="submit" value="Gem" class="btn btn-primary" />
    </div>
}